const themes = {
	typewriter: {
		background: '',
		accents: [''],
		primaryFont: '',
		headerFont: '',
		styleActionText: (text) => '[' + text.toUpperCase() + ']',
	},
	code: {
		background: '',
		accents: [''],
		primaryFont: '',
		headerFont: '',
		styleActionText: (text) => '(' + text.toLowerCase() + ')',
	},
};

export default themes;
